#!/bin/bash

# OKR 后端部署配置文件 - 服务器 2
# 服务器: 47.243.127.167 (测试/备用服务器)
# SSH 端口: 29722
# 部署应用: OKR 后端 API 服务

# 服务器配置
export SERVER_IP="47.243.127.167"
export SERVER_USER="root"
export SERVER_PORT="29722"
export SERVER_SSH_KEY="" # 如果使用密钥认证，在这里指定密钥路径

# 目录配置
export BACKEND_DIR="/www/wwwroot/okr-server"
export BACKUP_DIR="/www/backup/okr-server"

# PM2 应用名称
export BACKEND_PM2_NAME="okr-server"

# 端口配置
export BACKEND_PORT="3010"
export API_BASE_URL="http://okr.share1.gerenukagro.com"

# 域名配置
export DOMAIN="47.243.127.167"

# 备份保留数量
export BACKUP_KEEP_COUNT="5"

# 健康检查配置
export HEALTH_CHECK_URL="http://${DOMAIN}:3010/api/v1/health"
export HEALTH_CHECK_TIMEOUT="30"

# 构建配置
export NODE_ENV="production"
export BUILD_COMMAND="npm run build"
export INSTALL_COMMAND="npm install"

# 数据库迁移配置
export RUN_MIGRATION="true"
export MIGRATION_COMMAND="npm run migration:run"

# rsync 排除文件列表
export RSYNC_EXCLUDES=(
    'node_modules'
    '.git'
    'logs'
    '.env'
    '.env.local'
    'deploy'
    '*.log'
    '.DS_Store'
    'Thumbs.db'
    '.vscode'
    '.idea'
    'coverage'
    'test'
    'dist'
)

# 日志配置
export LOG_DIR="/www/wwwroot/okr-server/logs"
export LOG_LEVEL="info"

# 可选：Slack/企业微信通知配置（如需要）
export NOTIFICATION_WEBHOOK=""
export NOTIFICATION_ENABLED="false"

# 服务器描述（用于日志输出）
export SERVER_DESC="测试/备用服务器 (47.243.127.167:29722)"
